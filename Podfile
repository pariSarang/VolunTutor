# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Volunteering App' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Volunteering App
  pod "Firebase/Firestore"
  pod "Firebase/Auth"
  target 'Volunteering AppTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Volunteering AppUITests' do
    # Pods for testing
  end

end
